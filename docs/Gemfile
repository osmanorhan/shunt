source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "jekyll-remote-theme"
gem "jekyll-seo-tag"

# Ruby 3.3+ no longer exposes some bundled/default gems unless they are
# declared, but older GitHub Pages dependencies still require them directly.
gem "base64"
gem "bigdecimal"
gem "rexml"
